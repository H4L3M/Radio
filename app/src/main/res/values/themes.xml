<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Radio" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">true</item>

        <item name="android:colorBackground">@color/bg_dark</item>

        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>

    </style>

    <style name="Theme.Radio.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Radio.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Radio.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.Radio.SplashScreen" parent="Theme.Radio.NoActionBar" />

    <!--  Modal  -->
    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
<!--        <item name="android:textViewStyle">@style/Theme.Nokhba.Dark.TextStyle</item>-->
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_main</item>
        <item name="behavior_peekHeight">512dp</item>
        <item name="android:layout_marginTop">@dimen/simple_dimen_s</item>
        <item name="android:layout_marginStart">@dimen/simple_dimen_s</item>
        <item name="android:layout_marginLeft">@dimen/simple_dimen_s</item>
        <item name="android:layout_marginEnd">@dimen/simple_dimen_s</item>
        <item name="android:layout_marginRight">@dimen/simple_dimen_s</item>
    </style>

    <dimen name="simple_dimen_s">4dp</dimen>
</resources>